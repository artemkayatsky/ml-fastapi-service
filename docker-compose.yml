version: '3.8'

services:
  ml-service:
    build: .
    container_name: ml-text-classifier
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped